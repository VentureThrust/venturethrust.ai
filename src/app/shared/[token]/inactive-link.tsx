// Moved to a shared component so both /shared/[token] and /view/[fileId]/[linkId]
// can use it. Kept as a re-export so existing imports keep working.
export { InactiveLink } from '@/components/inactive-link';
